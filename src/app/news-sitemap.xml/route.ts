import { getPublishedNews } from "@/lib/content-store";
export async function GET() { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://grimmfirepump.co.za"; const articles = await getPublishedNews(); const urls = articles.map((article) => `<url><loc>${base}/news/${article.slug}</loc><news:news><news:publication><news:name>GRIMM PUMP Africa</news:name><news:language>en</news:language></news:publication><news:publication_date>${new Date(article.publishedAt).toISOString()}</news:publication_date><news:title>${article.title.replace(/&/g, "&amp;")}</news:title></news:news></url>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:news="http://www.google.com/schemas/sitemap-news/0.9">${urls}</urlset>`, { headers: { "Content-Type": "application/xml; charset=utf-8", "Cache-Control": "public, s-maxage=3600" } }); }
