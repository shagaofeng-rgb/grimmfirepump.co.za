import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getNewsArticle, getPublishedNews, getPublishedProducts } from "@/lib/content-store";
export async function generateStaticParams() { return (await getPublishedNews()).map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const article = await getNewsArticle((await params).slug); return { title: article?.title ?? "News", description: article?.excerpt }; }
export default async function ArticlePage({ params }: { params: Promise<{ slug: string }> }) { const article = await getNewsArticle((await params).slug); if (!article) notFound(); const products = await getPublishedProducts(); const related = products.filter((item) => article.relatedProductIds.includes(item.id)); const jsonLd = { "@context": "https://schema.org", "@type": "Article", headline: article.title, description: article.excerpt, datePublished: article.publishedAt, dateModified: article.publishedAt, author: { "@type": "Organization", name: "GRIMM PUMP Africa" }, publisher: { "@type": "Organization", name: "Grimm Water Treatment (Zhejiang) Co.,Ltd." } }; return <article className="wrap article"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><Link href="/news" className="breadcrumb">← News & insights</Link><p className="eyebrow">{article.category}</p><h1>{article.title}</h1><p className="article-summary">{article.excerpt}</p><time dateTime={article.publishedAt}>Published {new Intl.DateTimeFormat("en", { dateStyle: "long" }).format(new Date(article.publishedAt))}</time><div className="article-content">{article.content.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div><section className="related"><h2>Related GRIMM PUMP systems</h2>{related.map((product) => <Link key={product.id} href={`/products/${product.slug}`}>{product.name} →</Link>)}</section><Link href="/contact" className="cta">Discuss your project →</Link></article>; }
