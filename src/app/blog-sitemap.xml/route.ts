import { getPublishedBlogPosts } from "@/lib/blog-store";
export async function GET() { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://grimmfirepump.co.za"; const posts = await getPublishedBlogPosts(); const urls = posts.map((post) => `<url><loc>${base}/blog/${post.slug}</loc><lastmod>${new Date(post.publishedAt).toISOString()}</lastmod></url>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls}</urlset>`, { headers: { "Content-Type": "application/xml; charset=utf-8", "Cache-Control": "public, s-maxage=3600" } }); }
