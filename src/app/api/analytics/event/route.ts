import { NextRequest, NextResponse } from "next/server";
import { getDatabase, hasDatabase } from "@/lib/database";
const allowedEvents = new Set(["page_view", "product_view", "generate_lead", "whatsapp_click", "email_click", "catalog_download"]);
export async function POST(request: NextRequest) { const body = await request.json().catch(() => null) as { path?: unknown; eventType?: unknown } | null; const path = typeof body?.path === "string" ? body.path.slice(0, 300) : ""; const eventType = typeof body?.eventType === "string" && allowedEvents.has(body.eventType) ? body.eventType : "page_view"; if (!path || !path.startsWith("/") || !hasDatabase()) return NextResponse.json({ success: true }); await getDatabase()`INSERT INTO site_events (path, event_type) VALUES (${path}, ${eventType})`; return NextResponse.json({ success: true }); }
