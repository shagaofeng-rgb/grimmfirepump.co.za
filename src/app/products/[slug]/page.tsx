import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getProduct, getPublishedProducts } from "@/lib/content-store";
export async function generateStaticParams() { return (await getPublishedProducts()).map(({ slug }) => ({ slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const product = await getProduct((await params).slug); return { title: product?.name ?? "Product", description: product?.summary }; }
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) { const product = await getProduct((await params).slug); if (!product) notFound(); return <div className="wrap product-page"><Link href="/products" className="breadcrumb">← Products</Link><div className="product-detail"><div className="detail-image"><Image src={product.image} alt={product.name} fill sizes="(max-width: 800px) 100vw, 52vw" /></div><div><p className="eyebrow">{product.category}</p><h1>{product.name}</h1><p className="page-lead">{product.summary}</p><ul className="check-list">{product.highlights.map((item) => <li key={item}>{item}</li>)}</ul><Link href={`/contact?product=${encodeURIComponent(product.name)}`} className="cta">Request this configuration →</Link></div></div><div className="detail-lower"><section><h2>Published technical information</h2><div className="spec-table">{product.specifications.map((spec) => <div key={spec.label}><b>{spec.label}</b><span>{spec.value}</span></div>)}</div></section><section><h2>Typical applications</h2><div className="tag-list">{product.applications.map((item) => <span key={item}>{item}</span>)}</div><p className="notice">Technical details shown here are based on the current main-site content. GRIMM PUMP should confirm the final configuration before quotation or order placement.</p></section></div></div>; }
