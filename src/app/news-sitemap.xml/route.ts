import { getPublishedNews } from "@/lib/content-store";
import { indexableResources } from "@/lib/resource-policy";
export async function GET() { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://grimmfirepump.co.za"; const articles = indexableResources(await getPublishedNews()); const urls = articles.map((article) => `<url><loc>${base}/news/${article.slug}</loc><lastmod>${new Date(article.updatedAt ?? article.publishedAt).toISOString()}</lastmod></url>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls}</urlset>`, { headers: { "Content-Type": "application/xml; charset=utf-8", "Cache-Control": "public, s-maxage=3600" } }); }
