"use client";

import Image from "next/image";
import { useState, type FormEvent, type ReactNode } from "react";
import { SeoReportPanel } from "@/components/seo-report-panel";

type Tab = "overview" | "products" | "categories" | "news" | "blog" | "automation" | "media" | "leads" | "forms" | "analytics" | "seo" | "pages" | "downloads" | "accounts" | "logs" | "settings";
const labels: Record<Tab, string> = { overview: "数据概览", products: "产品管理", categories: "产品分类", news: "新闻管理", blog: "Blog 文章管理", automation: "新闻自动化", media: "媒体资源", leads: "客户询盘", forms: "表单管理", analytics: "访问分析", seo: "SEO 管理", pages: "页面管理", downloads: "下载资料", accounts: "账号与权限", logs: "操作日志", settings: "系统设置" };
const descriptions: Partial<Record<Tab, string>> = { products: "创建并维护面向非洲市场的产品目录。", categories: "管理产品分类、URL 标识与排序。", news: "维护网站新闻和行业洞察草稿。", automation: "查看新闻自动发布任务的每日审计结果。", media: "登记图片、PDF、视频等已托管的媒体资源。", leads: "跟进客户线索、负责人、优先级和内部备注。", forms: "登记表单的接收邮箱和启用状态。", analytics: "查看最近 30 天真实页面访问数据。", pages: "维护页面名称与 SEO 标题、描述。", downloads: "管理目录、技术资料等公开下载链接。", accounts: "查看已建立的内部账号和角色。", logs: "审计后台新增、更新与删除操作。", settings: "保存网站运行参数与运营配置。" };
const modules: Partial<Record<Tab, string>> = { categories: "categories", automation: "automation", media: "media", forms: "forms", pages: "pages", downloads: "downloads", analytics: "analytics", accounts: "accounts", logs: "logs", settings: "settings" };

function Brand() { return <div className="admin-brand"><Image src="/brand/grimm-pump-logo.png" alt="GRIMM PUMP" width={48} height={48}/><span><strong>GRIMM PUMP</strong><small>AFRICA · MANAGEMENT</small></span></div>; }
function toSlug(value: string) { return value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, ""); }

export function AdminConsole({ configured }: { configured: boolean }) {
  const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [logged, setLogged] = useState(false);
  const [tab, setTab] = useState<Tab>("overview"); const [data, setData] = useState<Record<string, unknown>[]>([]); const [overview, setOverview] = useState<Record<string, number>>({});
  const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  async function login(event: FormEvent) { event.preventDefault(); setError(""); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ username, password }) }); const result = await response.json(); if (!response.ok) { setError(result.error ?? "登录失败"); return; } setPassword(""); setLogged(true); void load("overview"); }
  async function load(next: Tab) { setTab(next); setError(""); const endpoint = next === "overview" ? "/api/admin/overview" : next === "products" ? "/api/admin/products" : next === "news" ? "/api/admin/news" : next === "blog" ? "/api/admin/blog" : next === "leads" ? "/api/admin/leads" : modules[next] ? `/api/admin/management/${modules[next]}` : null; if (!endpoint) return; setLoading(true); const response = await fetch(endpoint); const result = await response.json(); setLoading(false); if (!response.ok) { setError(result.error ?? "无法读取数据"); return; } if (next === "overview") setOverview(result.data); else setData(result.data ?? []); }
  async function create(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const resource = modules[tab]; const endpoint = tab === "products" ? "/api/admin/products" : tab === "news" ? "/api/admin/news" : resource ? `/api/admin/management/${resource}` : null; if (!endpoint) return; const form = event.currentTarget; const response = await fetch(endpoint, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(form))) }); const result = await response.json(); if (!response.ok) { setError(result.error ?? "保存失败"); return; } form.reset(); void load(tab); }
  async function saveLead(id: string, form: HTMLFormElement) { const response = await fetch("/api/admin/leads", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id, ...Object.fromEntries(new FormData(form)) }) }); const result = await response.json(); if (!response.ok) { setError(result.error ?? "保存跟进失败"); return; } void load("leads"); }
  async function deleteLead(id: string) { if (!window.confirm("确认删除该线索？删除后不会再显示在后台。")) return; const response = await fetch(`/api/admin/leads?id=${encodeURIComponent(id)}`, { method: "DELETE" }); const result = await response.json(); if (!response.ok) { setError(result.error ?? "删除失败"); return; } void load("leads"); }
  async function deleteRecord(id: string) { const resource = tab === "products" ? "/api/admin/products" : tab === "news" ? "/api/admin/news" : tab === "blog" ? "/api/admin/blog" : modules[tab] ? `/api/admin/management/${modules[tab]}` : ""; if (!resource || !window.confirm("确认删除该记录？")) return; const response = await fetch(`${resource}?id=${encodeURIComponent(id)}`, { method: "DELETE" }); const result = await response.json(); if (!response.ok) { setError(result.error ?? "删除失败"); return; } void load(tab); }
  if (!configured) return <section className="admin-login-wrap"><div className="admin-login"><Brand/><h1>管理后台尚未启用</h1><p>生产环境尚未配置管理员账号。</p></div></section>;
  if (!logged) return <section className="admin-login-wrap"><form className="admin-login" onSubmit={login}><Brand/><p className="admin-kicker">安全登录</p><h1>非洲站中文管理后台</h1><p>仅限获得授权的内部运营人员使用。</p><label>管理员账号<input value={username} onChange={event => setUsername(event.target.value)} autoComplete="username" required/></label><label>管理员密码<input value={password} onChange={event => setPassword(event.target.value)} type="password" autoComplete="current-password" required/></label><button className="admin-primary">进入后台 →</button>{error && <p className="admin-form-message">{error}</p>}</form></section>;
  const formFields: Partial<Record<Tab, ReactNode>> = { products: <><label>产品名称<input name="name" required/></label><label>产品分类<select name="category"><option>Fire pump systems</option><option>Water supply</option><option>Mobile pumping</option><option>Drainage</option></select></label><label>产品简介<textarea name="summary" required/></label></>, news: <><label>新闻标题<input name="title" required/></label><label>分类<input name="category" required/></label><label>新闻摘要<textarea name="excerpt" required/></label></>, categories: <><label>分类名称<input name="name" required onBlur={event => { const slug = event.currentTarget.form?.elements.namedItem("slug") as HTMLInputElement | null; if (slug && !slug.value) slug.value = toSlug(event.currentTarget.value); }}/></label><label>URL 标识<input name="slug" required placeholder="fire-pump-systems"/></label><label>说明<textarea name="description"/></label></>, media: <><label>资源名称<input name="title" required/></label><label>资源网址<input name="url" type="url" required/></label><label>替代文字<input name="alt"/></label><label>类型<select name="type"><option value="image">图片</option><option value="document">文档</option><option value="video">视频</option></select></label></>, forms: <><label>表单名称<input name="name" required/></label><label>接收邮箱<input name="email" type="email" required/></label></>, pages: <><label>页面路径<input name="path" placeholder="/about" required/></label><label>页面名称<input name="label" required/></label><label>SEO 标题<input name="title"/></label><label>SEO 描述<textarea name="description"/></label></>, downloads: <><label>资料名称<input name="title" required/></label><label>公开下载网址<input name="url" type="url" required/></label><label>说明<textarea name="description"/></label></>, settings: <><label>设置键<input name="key" required placeholder="company.whatsapp"/></label><label>设置值<textarea name="value" required/></label></> };
  return <div className="admin-workspace"><header className="admin-topbar"><Brand/><button className="admin-logout" onClick={async () => { await fetch("/api/admin/logout", { method: "POST" }); setLogged(false); }}>退出登录</button></header><div className="admin-layout"><aside className="admin-sidebar"><p>网站运营后台</p>{(Object.keys(labels) as Tab[]).map(item => <button key={item} className={tab === item ? "selected" : ""} onClick={() => void load(item)}>{labels[item]}</button>)}</aside><main className="admin-content"><div className="admin-content-heading"><p className="admin-kicker">GRIMM PUMP AFRICA · OPERATIONS</p><h1>{labels[tab]}</h1></div>{descriptions[tab] && <p className="admin-intro">{descriptions[tab]}</p>}{error && <p className="admin-form-message">{error}</p>}{tab === "overview" ? <div className="admin-metrics">{Object.entries(overview).map(([key, value]) => <article key={key}><b>{value}</b><span>{{ products: "产品", categories: "分类", news: "新闻", leads: "询盘", views: "近 30 日访问", downloads: "下载资料" }[key] ?? key}</span></article>)}</div> : tab === "seo" ? <SeoReportPanel/> : <>{formFields[tab] && <form className="admin-form" onSubmit={create}>{formFields[tab]}<button className="admin-primary">保存</button></form>}<div className="admin-table">{loading ? <div className="admin-empty">正在读取真实数据…</div> : tab === "leads" && data.length ? data.map((row, index) => <form className="admin-lead-card" key={String(row.id ?? index)} onSubmit={event => { event.preventDefault(); void saveLead(String(row.id), event.currentTarget); }}><div className="admin-lead-heading"><div><b>{String(row.name ?? "未命名客户")}</b><span>{String(row.company ?? "—")} · {String(row.country ?? "—")} · {String(row.email ?? "—")}</span></div><span className="admin-score">{String(row.productInterest ?? "一般询盘")}</span></div><p>{String(row.message ?? "未填写需求内容")}</p><span className="admin-lead-meta">{String(row.sourcePage ?? "网站表单")} · {row.createdAt ? new Date(String(row.createdAt)).toLocaleString("zh-CN") : ""}</span><div className="admin-lead-grid"><label>跟进状态<select name="status" defaultValue={String(row.status ?? "new")}><option value="new">新询盘</option><option value="contacted">已联系</option><option value="qualified">已判定</option><option value="quoted">已报价</option><option value="closed">已成交 / 关闭</option><option value="spam">垃圾线索</option><option value="archived">已归档</option></select></label><label>意向等级<select name="priority" defaultValue={String(row.priority ?? "unrated")}><option value="A">A 高意向</option><option value="B">B 中意向</option><option value="C">C 低意向</option><option value="unrated">未判断</option></select></label><label>销售负责人<input name="salesOwner" defaultValue={String(row.salesOwner ?? "")}/></label><label>内部备注<textarea name="internalNote" defaultValue={String(row.internalNote ?? "")}/></label></div><div className="admin-row-actions"><button className="admin-primary">保存跟进</button><button type="button" className="admin-danger" onClick={() => void deleteLead(String(row.id))}>删除线索</button></div></form>) : data.length ? data.map((row, index) => <article key={String(row.id ?? index)}><div><b>{String(row.name ?? row.title ?? row.path ?? row.email ?? row.action ?? "记录")}</b><span>{Object.entries(row).filter(([key]) => !["id", "name", "title", "path", "email", "action"].includes(key)).slice(0, 3).map(([key, value]) => `${key}: ${typeof value === "object" ? JSON.stringify(value) : String(value)}`).join(" · ")}</span></div>{!["analytics", "logs", "automation"].includes(tab) && <button className="admin-danger" type="button" onClick={() => void deleteRecord(String(row.id ?? row.key))}>删除</button>}</article>) : <div className="admin-empty">暂时没有数据；首次保存或产生访问后会在这里显示。</div>}</div></>}</main></div></div>;
}
