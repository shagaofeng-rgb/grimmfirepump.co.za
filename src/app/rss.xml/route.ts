import { getPublishedNews } from "@/lib/content-store";
function esc(value: string) { return value.replace(/[<>&'"]/g, (char) => ({ "<": "&lt;", ">": "&gt;", "&": "&amp;", "'": "&apos;", '"': "&quot;" })[char] ?? char); }
export async function GET() { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://grimmfirepump.co.za"; const articles = await getPublishedNews(); const items = articles.slice(0, 30).map((article) => `<item><title>${esc(article.title)}</title><link>${base}/news/${article.slug}</link><guid>${base}/news/${article.slug}</guid><pubDate>${new Date(article.publishedAt).toUTCString()}</pubDate><description>${esc(article.excerpt)}</description></item>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>GRIMM PUMP Africa News</title><link>${base}/news</link><description>Source-linked pumping and water-project context for Africa.</description>${items}</channel></rss>`, { headers: { "Content-Type": "application/rss+xml; charset=utf-8", "Cache-Control": "public, s-maxage=3600" } }); }
